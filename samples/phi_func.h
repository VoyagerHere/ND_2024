#include <vector>

std::vector<double> phi_func
(
  std::vector<double> g,
  std::vector<int> n,
  std::vector<int> d
) 
{
  std::vector<double> exch(g.size());
  std::vector<double> dy_dt(g.size());
  for (int i = 0; i < g.size(); i++) {
    
  }
}